# Prerequisites  
- `node >=18`
- `pnpm >=9`
- Docker, for the local Postgres service

# Run everything
1. `pnpm install`
2. `pnpm dev:all`

You can also use `make dev`, which delegates to `pnpm dev:all`.


